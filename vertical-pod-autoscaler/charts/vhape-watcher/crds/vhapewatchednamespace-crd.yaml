apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: vhapewatchednamespaces.autoscaling.vhape.io
spec:
  group: autoscaling.vhape.io
  scope: Cluster
  names:
    plural: vhapewatchednamespaces
    singular: vhapewatchednamespace
    kind: VhapeWatchedNamespace
    listKind: VhapeWatchedNamespaceList
  versions:
    - name: v1alpha1
      served: true
      storage: true
      additionalPrinterColumns:
        - jsonPath: .spec.vhapePolicyRef.namespace
          name: PolicyNamespace
          type: string
        - jsonPath: .spec.vhapePolicyRef.name
          name: PolicyName
          type: string
        - jsonPath: .spec.vpaUpdateMode
          name: UpdateMode
          type: string
      schema:
        openAPIV3Schema:
          type: object
          description: Marks a Kubernetes namespace as eligible for VHAPE Watcher management.
          required:
            - spec
          properties:
            spec:
              type: object
              required:
                - vhapePolicyRef
                - vpaUpdateMode
              properties:
                vhapePolicyRef:
                  type: object
                  required:
                    - namespace
                    - name
                  properties:
                    namespace:
                      type: string
                    name:
                      type: string
                vpaUpdateMode:
                  type: string
                  enum:
                    - Off
                    - Initial
                    - Recreate
                    - InPlaceOrRecreate
